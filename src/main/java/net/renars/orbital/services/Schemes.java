package net.renars.orbital.services;

import net.renars.orbital.team.TeamScheme;
import net.renars.orbital.user.UserScheme;

public class Schemes {
    public static final TeamScheme TEAM_SCHEME = new TeamScheme();
    public static final UserScheme USER_SCHEME = new UserScheme();
}
